

      <%= form_with url: "/rating", method: :post do |form| %>

        <label>Rating: <%= form.select :quantity, ['GOOD', 'BAD', 'AVERAGE'] %></label>

        <%= form.submit "Add Rating" %>
      <% end %>

