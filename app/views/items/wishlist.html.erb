<!DOCTYPE html>
<html>
    <head>
        <title>The Generics Store | Save List</title>
        <meta name="description" content="Save List Page">
        <script src="store.js" async></script>
    </head>

    <body>
        <section class="container content-section">

            <div class="shop-items">

                <% if @savelist.empty? %>
                <h3>Your saved list is empty!</h3>
                <% end %>
                <% @savelist.each do |i| %>

                    <div class="shop-item">
                        <span class="shop-item-title"><%= i.title %></span>
                        <a href=<%= itemDetails_path(i) %>><%= image_tag i.image, :size => "300x300" %></a>

                        <div class="shop-item-details">
                            <span class="shop-item-price">$<%= i.price %></span>
                            <span class="shop-item-price"><%= i.category %></span>

                            <a href="<%=itemRemove_path(i) %>">
                            <button class="btn btn-primary shop-item-button" type="button">Remove from save list</button>
                            </a>
                        </div>
                    </div>


                <% end %>
            </div>
        </section>
    </body>
</html>